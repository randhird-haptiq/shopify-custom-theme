<div class="logo">
    {% for block in section.blocks %}
      {% if block.type == 'logo' %}
        <a href="{{ shop.url }}">
          <img src="{{ block.settings.logo | img_url: 'medium' }}" alt="Logo" height="" width="">
        </a>
      {% endif %}
    {% endfor %}
  </div>